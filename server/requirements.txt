# NOTE (Windows + NVIDIA GPU): install the CUDA build of torch FIRST, or pip
# will pull the CPU-only wheel as a dependency of mokuro:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
mokuro>=0.2.4
fastapi
uvicorn[standard]
